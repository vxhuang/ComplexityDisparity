by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsInMAP_nz_HIA <- resultsInMAP_nz_HIA %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsInMAP_nz_HIA <- resultsInMAP_nz_HIA %>% mutate(deaths_WNH = Value * (1 - 1 / exp(beta * TotalPM25)) * WNH,
deaths_Hispanic = Value * (1 - 1 / exp(beta * TotalPM25)) * Hispanic,
deaths_BNH = Value * (1 - 1 / exp(beta * TotalPM25)) * BNH,
deaths_Other = Value * (1 - 1 / exp(beta * TotalPM25)) * Other)
resultsInMAP_nz_HIA <- resultsInMAP_nz_HIA %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsInMAP_nz_HIA %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
resultsInMAP_nz_di <- resultsInMAP_nz_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsInMAP_nz_di <- resultsInMAP_nz_di %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsInMAP_nz_di <- resultsInMAP_nz_di %>% mutate(deaths_WNH = Value * (1 - 1 / exp(beta_di_nhwa * TotalPM25)) * WNH,
deaths_Hispanic = Value * (1 - 1 / exp(beta_di_hispanic * TotalPM25)) * Hispanic,
deaths_BNH = Value * (1 - 1 / exp(beta_di_black * TotalPM25)) * BNH,
deaths_Other = Value * (1 - 1 / exp(beta_di_asian * TotalPM25)) * Other)
resultsInMAP_nz_di <- resultsInMAP_nz_di %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsInMAP_nz_di %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
# different uniform
resultsInMAP_nz_HIA <- resultsInMAP_nz_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsInMAP_nz_HIA <- resultsInMAP_nz_HIA %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsInMAP_nz_HIA <- resultsInMAP_nz_HIA %>% left_join(y0_race, by = c("AGEGRP")) %>%
mutate(deaths_WNH = Value * (1 - 1 / exp(beta * TotalPM25)) * WNH * wa,
deaths_Hispanic = Value * (1 - 1 / exp(beta * TotalPM25)) * Hispanic * his,
deaths_BNH = Value * (1 - 1 / exp(beta * TotalPM25)) * BNH * ba,
deaths_Other = Value * (1 - 1 / exp(beta * TotalPM25)) * Other * other)
resultsInMAP_nz_HIA <- resultsInMAP_nz_HIA %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsInMAP_nz_HIA %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
resultsInMAP_nz_di <- resultsInMAP_nz_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsInMAP_nz_di <- resultsInMAP_nz_di %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsInMAP_nz_di <- resultsInMAP_nz_di %>% left_join(y0_race, by = c("AGEGRP")) %>%
mutate(deaths_WNH = Value * (1 - 1 / exp(beta_di_nhwa * TotalPM25)) * WNH * wa,
deaths_Hispanic = Value * (1 - 1 / exp(beta_di_hispanic * TotalPM25)) * Hispanic * his,
deaths_BNH = Value * (1 - 1 / exp(beta_di_black * TotalPM25)) * BNH * ba,
deaths_Other = Value * (1 - 1 / exp(beta_di_asian * TotalPM25)) * Other * other)
resultsInMAP_nz_di <- resultsInMAP_nz_di %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsInMAP_nz_di %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
# uniform uniform
resultsISRM_nz_HIA <- resultsISRM_nz_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsISRM_nz_HIA <- resultsISRM_nz_HIA %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsISRM_nz_HIA <- resultsISRM_nz_HIA %>% mutate(deaths_WNH = Value * (1 - 1 / exp(beta * TotalPM25)) * WNH,
deaths_Hispanic = Value * (1 - 1 / exp(beta * TotalPM25)) * Hispanic,
deaths_BNH = Value * (1 - 1 / exp(beta * TotalPM25)) * BNH,
deaths_Other = Value * (1 - 1 / exp(beta * TotalPM25)) * Other)
resultsISRM_nz_HIA <- resultsISRM_nz_HIA %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsISRM_nz_HIA %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
resultsISRM_nz_di <- resultsISRM_nz_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsISRM_nz_di <- resultsISRM_nz_di %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsISRM_nz_di <- resultsISRM_nz_di %>% mutate(deaths_WNH = Value * (1 - 1 / exp(beta_di_nhwa * TotalPM25)) * WNH,
deaths_Hispanic = Value * (1 - 1 / exp(beta_di_hispanic * TotalPM25)) * Hispanic,
deaths_BNH = Value * (1 - 1 / exp(beta_di_black * TotalPM25)) * BNH,
deaths_Other = Value * (1 - 1 / exp(beta_di_asian * TotalPM25)) * Other)
resultsISRM_nz_di <- resultsISRM_nz_di %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsISRM_nz_di %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
# different uniform
resultsISRM_nz_HIA <- resultsISRM_nz_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsISRM_nz_HIA <- resultsISRM_nz_HIA %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsISRM_nz_HIA <- resultsISRM_nz_HIA %>% left_join(y0_race, by = c("AGEGRP")) %>%
mutate(deaths_WNH = Value * (1 - 1 / exp(beta * TotalPM25)) * WNH * wa,
deaths_Hispanic = Value * (1 - 1 / exp(beta * TotalPM25)) * Hispanic * his,
deaths_BNH = Value * (1 - 1 / exp(beta * TotalPM25)) * BNH * ba,
deaths_Other = Value * (1 - 1 / exp(beta * TotalPM25)) * Other * other)
resultsISRM_nz_HIA <- resultsISRM_nz_HIA %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsISRM_nz_HIA %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
resultsISRM_nz_di <- resultsISRM_nz_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsISRM_nz_di <- resultsISRM_nz_di %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsISRM_nz_di <- resultsISRM_nz_di %>% left_join(y0_race, by = c("AGEGRP")) %>%
mutate(deaths_WNH = Value * (1 - 1 / exp(beta_di_nhwa * TotalPM25)) * WNH * wa,
deaths_Hispanic = Value * (1 - 1 / exp(beta_di_hispanic * TotalPM25)) * Hispanic * his,
deaths_BNH = Value * (1 - 1 / exp(beta_di_black * TotalPM25)) * BNH * ba,
deaths_Other = Value * (1 - 1 / exp(beta_di_asian * TotalPM25)) * Other * other)
resultsISRM_nz_di <- resultsISRM_nz_di %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsISRM_nz_di %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
# uniform uniform
jie_wrf_race2_HIA <- jie_wrf_race2_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
jie_wrf_race2_HIA <- jie_wrf_race2_HIA %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
jie_wrf_race2_HIA <- jie_wrf_race2_HIA %>% mutate(deaths_WNH = Value * (1 - 1 / exp(beta * TotalPM25)) * WNH,
deaths_Hispanic = Value * (1 - 1 / exp(beta * TotalPM25)) * Hispanic,
deaths_BNH = Value * (1 - 1 / exp(beta * TotalPM25)) * BNH,
deaths_Other = Value * (1 - 1 / exp(beta * TotalPM25)) * Other)
jie_wrf_race2_HIA <- jie_wrf_race2_HIA %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
jie_wrf_race2_HIA %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
jie_wrf_race2_di <- jie_wrf_race2_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
jie_wrf_race2_di <- jie_wrf_race2_di %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
jie_wrf_race2_di <- jie_wrf_race2_di %>% mutate(deaths_WNH = Value * (1 - 1 / exp(beta_di_nhwa * TotalPM25)) * WNH,
deaths_Hispanic = Value * (1 - 1 / exp(beta_di_hispanic * TotalPM25)) * Hispanic,
deaths_BNH = Value * (1 - 1 / exp(beta_di_black * TotalPM25)) * BNH,
deaths_Other = Value * (1 - 1 / exp(beta_di_asian * TotalPM25)) * Other)
jie_wrf_race2_di <- jie_wrf_race2_di %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
jie_wrf_race2_di %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
# different uniform
jie_wrf_race2_HIA <- jie_wrf_race2_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
jie_wrf_race2_HIA <- jie_wrf_race2_HIA %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
jie_wrf_race2_HIA <- jie_wrf_race2_HIA %>% left_join(y0_race, by = c("AGEGRP")) %>%
mutate(deaths_WNH = Value * (1 - 1 / exp(beta * TotalPM25)) * WNH * wa,
deaths_Hispanic = Value * (1 - 1 / exp(beta * TotalPM25)) * Hispanic * his,
deaths_BNH = Value * (1 - 1 / exp(beta * TotalPM25)) * BNH * ba,
deaths_Other = Value * (1 - 1 / exp(beta * TotalPM25)) * Other * other)
jie_wrf_race2_HIA <- jie_wrf_race2_HIA %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
jie_wrf_race2_HIA %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
jie_wrf_race2_di <- jie_wrf_race2_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
jie_wrf_race2_di <- jie_wrf_race2_di %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
jie_wrf_race2_di <- jie_wrf_race2_di %>% left_join(y0_race, by = c("AGEGRP")) %>%
mutate(deaths_WNH = Value * (1 - 1 / exp(beta_di_nhwa * TotalPM25)) * WNH * wa,
deaths_Hispanic = Value * (1 - 1 / exp(beta_di_hispanic * TotalPM25)) * Hispanic * his,
deaths_BNH = Value * (1 - 1 / exp(beta_di_black * TotalPM25)) * BNH * ba,
deaths_Other = Value * (1 - 1 / exp(beta_di_asian * TotalPM25)) * Other * other)
jie_wrf_race2_di <- jie_wrf_race2_di %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
jie_wrf_race2_di %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
# uniform uniform
resultsInMAP_race2_HIA <- resultsInMAP_race2_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsInMAP_race2_HIA <- resultsInMAP_race2_HIA %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsInMAP_race2_HIA <- resultsInMAP_race2_HIA %>% mutate(deaths_WNH = Value * (1 - 1 / exp(beta * TotalPM25)) * WNH,
deaths_Hispanic = Value * (1 - 1 / exp(beta * TotalPM25)) * Hispanic,
deaths_BNH = Value * (1 - 1 / exp(beta * TotalPM25)) * BNH,
deaths_Other = Value * (1 - 1 / exp(beta * TotalPM25)) * Other)
resultsInMAP_race2_HIA <- resultsInMAP_race2_HIA %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsInMAP_race2_HIA %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
resultsInMAP_race2_di <- resultsInMAP_race2_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsInMAP_race2_di <- resultsInMAP_race2_di %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsInMAP_race2_di <- resultsInMAP_race2_di %>% mutate(deaths_WNH = Value * (1 - 1 / exp(beta_di_nhwa * TotalPM25)) * WNH,
deaths_Hispanic = Value * (1 - 1 / exp(beta_di_hispanic * TotalPM25)) * Hispanic,
deaths_BNH = Value * (1 - 1 / exp(beta_di_black * TotalPM25)) * BNH,
deaths_Other = Value * (1 - 1 / exp(beta_di_asian * TotalPM25)) * Other)
resultsInMAP_race2_di <- resultsInMAP_race2_di %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsInMAP_race2_di %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
# different uniform
resultsInMAP_race2_HIA <- resultsInMAP_race2_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsInMAP_race2_HIA <- resultsInMAP_race2_HIA %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsInMAP_race2_HIA <- resultsInMAP_race2_HIA %>% left_join(y0_race, by = c("AGEGRP")) %>%
mutate(deaths_WNH = Value * (1 - 1 / exp(beta * TotalPM25)) * WNH * wa,
deaths_Hispanic = Value * (1 - 1 / exp(beta * TotalPM25)) * Hispanic * his,
deaths_BNH = Value * (1 - 1 / exp(beta * TotalPM25)) * BNH * ba,
deaths_Other = Value * (1 - 1 / exp(beta * TotalPM25)) * Other * other)
resultsInMAP_race2_HIA <- resultsInMAP_race2_HIA %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsInMAP_race2_HIA %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
resultsInMAP_race2_di <- resultsInMAP_race2_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsInMAP_race2_di <- resultsInMAP_race2_di %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsInMAP_race2_di <- resultsInMAP_race2_di %>% left_join(y0_race, by = c("AGEGRP")) %>%
mutate(deaths_WNH = Value * (1 - 1 / exp(beta_di_nhwa * TotalPM25)) * WNH * wa,
deaths_Hispanic = Value * (1 - 1 / exp(beta_di_hispanic * TotalPM25)) * Hispanic * his,
deaths_BNH = Value * (1 - 1 / exp(beta_di_black * TotalPM25)) * BNH * ba,
deaths_Other = Value * (1 - 1 / exp(beta_di_asian * TotalPM25)) * Other * other)
resultsInMAP_race2_di <- resultsInMAP_race2_di %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsInMAP_race2_di %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
# uniform uniform
resultsISRM_race2_HIA <- resultsISRM_race2_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsISRM_race2_HIA <- resultsISRM_race2_HIA %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsISRM_race2_HIA <- resultsISRM_race2_HIA %>% mutate(deaths_WNH = Value * (1 - 1 / exp(beta * TotalPM25)) * WNH,
deaths_Hispanic = Value * (1 - 1 / exp(beta * TotalPM25)) * Hispanic,
deaths_BNH = Value * (1 - 1 / exp(beta * TotalPM25)) * BNH,
deaths_Other = Value * (1 - 1 / exp(beta * TotalPM25)) * Other)
resultsISRM_race2_HIA <- resultsISRM_race2_HIA %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsISRM_race2_HIA %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
resultsISRM_race2_di <- resultsISRM_race2_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsISRM_race2_di <- resultsISRM_race2_di %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsISRM_race2_di <- resultsISRM_race2_di %>% mutate(deaths_WNH = Value * (1 - 1 / exp(beta_di_nhwa * TotalPM25)) * WNH,
deaths_Hispanic = Value * (1 - 1 / exp(beta_di_hispanic * TotalPM25)) * Hispanic,
deaths_BNH = Value * (1 - 1 / exp(beta_di_black * TotalPM25)) * BNH,
deaths_Other = Value * (1 - 1 / exp(beta_di_asian * TotalPM25)) * Other)
resultsISRM_race2_di <- resultsISRM_race2_di %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsISRM_race2_di %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
# different uniform
resultsISRM_race2_HIA <- resultsISRM_race2_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsISRM_race2_HIA <- resultsISRM_race2_HIA %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsISRM_race2_HIA <- resultsISRM_race2_HIA %>% left_join(y0_race, by = c("AGEGRP")) %>%
mutate(deaths_WNH = Value * (1 - 1 / exp(beta * TotalPM25)) * WNH * wa,
deaths_Hispanic = Value * (1 - 1 / exp(beta * TotalPM25)) * Hispanic * his,
deaths_BNH = Value * (1 - 1 / exp(beta * TotalPM25)) * BNH * ba,
deaths_Other = Value * (1 - 1 / exp(beta * TotalPM25)) * Other * other)
resultsISRM_race2_HIA <- resultsISRM_race2_HIA %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsISRM_race2_HIA %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
resultsISRM_race2_di <- resultsISRM_race2_cty %>% left_join(pop_ssp52050_for_y0,
by = c("STATEFP" = "STATE", "COUNTYFP" = "COUNTY"))
resultsISRM_race2_di <- resultsISRM_race2_di %>% left_join(y0 %>% filter(year == 2050),
by = c("STATEFP" = "Column", "COUNTYFP" = "Row", "AGEGRP" = "Start.Age"))
resultsISRM_race2_di <- resultsISRM_race2_di %>% left_join(y0_race, by = c("AGEGRP")) %>%
mutate(deaths_WNH = Value * (1 - 1 / exp(beta_di_nhwa * TotalPM25)) * WNH * wa,
deaths_Hispanic = Value * (1 - 1 / exp(beta_di_hispanic * TotalPM25)) * Hispanic * his,
deaths_BNH = Value * (1 - 1 / exp(beta_di_black * TotalPM25)) * BNH * ba,
deaths_Other = Value * (1 - 1 / exp(beta_di_asian * TotalPM25)) * Other * other)
resultsISRM_race2_di <- resultsISRM_race2_di %>% left_join(urban_ssp5, by = "fips") %>%
mutate(URB = TOT_POP * POPPCT_URB, RUR = TOT_POP * (1 - POPPCT_URB),
deaths_URB = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * POPPCT_URB,
deaths_RUR = (deaths_WNH + deaths_Hispanic + deaths_BNH + deaths_Other) * (1 - POPPCT_URB))
resultsISRM_race2_di %>%
mutate(pop_white = URB, pop_nonwhite = RUR,
deaths_white = deaths_URB, deaths_nonwhite = deaths_RUR) %>%
select(STATEFP, COUNTYFP, fips, TotalPM25, pop_white, pop_nonwhite, year, deaths_white, deaths_nonwhite) %>%
group_by(STATEFP, COUNTYFP, fips, TotalPM25, year) %>%
summarise(pop_white = sum(pop_white), pop_nonwhite = sum(pop_nonwhite),
deaths_white = sum(deaths_white), deaths_nonwhite = sum(deaths_nonwhite), .groups = "drop") %>%
select(TotalPM25, pop_white, pop_nonwhite, deaths_white, deaths_nonwhite) %>% drop_na() %>%
summarise(pm_white = weighted.mean(TotalPM25, pop_white, na.rm = T), pm_nonwhite = weighted.mean(TotalPM25, pop_nonwhite, na.rm = T),
pop_white = sum(pop_white, na.rm = T), pop_nonwhite = sum(pop_nonwhite, na.rm = T),
deaths_white = sum(deaths_white, na.rm = T), deaths_nonwhite = sum(deaths_nonwhite, na.rm = T)) %>%
mutate(dr_white = deaths_white / pop_white, dr_nonwhite = deaths_nonwhite / pop_nonwhite) %>% drop_na()
ggplot() +
geom_density(data = dt_fig_3c,
mapping = aes(x = TotalPM25, weight = wa,
color = scenario, linetype = scenario, linewidth = scenario),
adjust = 5, key_glyph = "path")
ggplot() +
geom_density(data = dt_fig_3c,
mapping = aes(x = TotalPM25, weight = wa,
color = scenario, linetype = scenario, linewidth = scenario),
adjust = 5)
y0_race
read_xlsx("~/Documents/PSU/2023 Spring/HIA_County/y0_race_his_1yr.xlsx", sheet = 1)
tmp <- read_xlsx("~/Documents/PSU/2023 Spring/HIA_County/y0_race_his_1yr.xlsx", sheet = 1)
unique(tmp$Year)
